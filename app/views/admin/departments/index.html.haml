%h1 Listing departments

%table.table.table-striped.table-bordered.table-hover.table-sm
  %thead
    %tr
      %th Name
      %th{colspan: 3} Actions

  %tbody
    - @departments.each do |department|
      %tr
        %td= department.name
        %td= link_to 'Show', admin_department_path(department)
        %td= link_to 'Edit', edit_admin_department_path(department)
        %td= link_to 'Destroy', admin_department_path(department), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Department', new_admin_department_path
