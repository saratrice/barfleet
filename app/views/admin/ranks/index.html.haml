%h1 Listing admin_ranks

%table.table.table-striped.table-bordered.table-hover.table-sm
  %thead
    %tr
      %th Name
      %th{colspan: 3} Actions

  %tbody
    - @ranks.each do |rank|
      %tr
        %td= rank.name
        %td= link_to 'Show', admin_rank_path(rank)
        %td= link_to 'Edit', edit_admin_rank_path(rank)
        %td= link_to 'Destroy', admin_rank_path(rank), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Rank', new_admin_rank_path
